# Title-shape library (rotate — never the same shape twice in a row, or within the last 6 videos)

The producer picks ONE shape per video that was NOT used in the last 6 videos (check `variation_ledger.md`).
All stay TRUE + brand-safe (no "guaranteed/overnight/cure") and keep the deception/curiosity + personal-
stakes DNA. `<X>` = the topic noun; `<n>` = a number; `<money>` = a concrete cost. Record the shape id used.

| id | Type | Shape | Example |
|---|---|---|---|
| T01 | deception | Your `<X>` is lying to you | Your credit card is lying to you |
| T02 | wrong | You've been using `<X>` wrong your whole life | You've been using your debit card wrong… |
| T03 | truth | The truth about `<X>` nobody tells you | The truth about credit scores nobody tells you |
| T04 | secret | Nobody tells you this about `<X>` | Nobody tells you this about savings accounts |
| T05 | myth | Why everything you know about `<X>` is wrong | Why everything you know about APR is wrong |
| T06 | reframe | `<X>` is not what you think | Your bank statement is not what you think |
| T07 | never-again | You'll never look at `<X>` the same again | You'll never look at your phone bill the same |
| T08 | number-mistake | The `<n>` mistake everyone makes with `<X>` | The #1 mistake everyone makes with credit cards |
| T09 | insider | What banks don't want you to know about `<X>` | What banks don't want you to know about overdrafts |
| T10 | stop | Stop `<doing X>` — here's why | Stop paying your card on the due date — here's why |
| T11 | cost | `<X>` is quietly costing you `<money>` | That subscription is quietly costing you $200/yr |
| T12 | real-reason | The real reason `<surprising fact>` | The real reason your score dropped after you paid |
| T13 | listicle | `<n>` things you're doing wrong with `<X>` | 3 things you're doing wrong with your savings |
| T14 | question | Is your `<X>` secretly `<bad thing>`? | Is your savings account secretly losing you money? |
| T15 | how-really | How `<X>` actually works (it's not what you think) | How your credit limit actually works |
| T16 | hidden | The hidden `<thing>` inside your `<X>` | The hidden fee inside your phone bill |
| T17 | comparison | `<A>` vs `<B>`: which is actually `<better>`? | Due date vs statement date: which actually matters? |
| T18 | did-you-know | Did you know your `<X>` does this? | Did you know your bank reorders your charges? |
| T19 | small-thing | This tiny `<thing>` is why you're `<losing>` | This one date is why your score keeps dropping |
| T20 | trick | The `<X>` trick banks use on you | The minimum-payment trick banks use on you |
| T21 | overpay | You're overpaying for `<X>` — here's the fix | You're overpaying on car insurance — here's the fix |
| T22 | story | I `<did X>` so you don't have to | I read the fine print so you don't have to |
| T23 | scam-ish | `<Everyday X>` is basically a scam | Gift cards are basically a scam |
| T24 | means | Here's what `<X>` really means | Here's what "available balance" really means |
| T25 | warning | If you have `<X>`, watch this | If you have a savings account, watch this |

**Rotation rule:** never reuse a shape id from the last 6 rows of `variation_ledger.md`; spread across TYPES
too (don't do 3 "deception" types in a row even with different ids). Prefer a shape that fits the chosen
FORMAT (e.g. `listicle` title T13 with the listicle format; `story` title T22 with the story format).
