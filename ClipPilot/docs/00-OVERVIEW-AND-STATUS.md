# 00 — Overview, Status & Document Roadmap

**Project:** ClipPilot (working title) — native Windows app, Claude-as-brain via MCP, for automated clipping + Shorts/Reels.
**Owner:** Priyansh
**Last updated:** 2026-06-23

---

## Operating rule for every work session / loop iteration

1. **PLAN → REVIEW → BUILD.** No application code until docs `01`–`05` below are complete and the owner has reviewed the open decisions in §"Open decisions".
2. The deep-research evidence lands in [`research/`](research/). The plan docs are written **from** that evidence, with the adversarial fact-checks reconciled in (use the conservative/verified number when research and the skeptic disagreed).
3. Keep the **honesty contract**: clearly separate (a) *technically buildable*, (b) *actually makes money*, and (c) *allowed to be 100% hands-off without bans*. These three are NOT the same.

## Current status

| Item | State |
|------|-------|
| Tool inventory (30 repos) | ✅ full due-diligence table done → [`03-tool-inventory.md`](03-tool-inventory.md) |
| Deep-research workflow (`wpyeupafa`) | ✅ complete (11 agents) → raw evidence in [`research/00-SYNTHESIS.md`](research/00-SYNTHESIS.md) |
| Project skeleton | ✅ created (`README.md`, `docs/`, `docs/research/`) |
| Planning docs `01`–`05` | ✅ **all written** |
| Owner gating decisions | ✅ **answered 2026-06-22** → see [`06-decisions-and-product.md`](06-decisions-and-product.md) |
| App build (`src/`) | ✅ **Phase 0 complete & tested** — stdlib-only core engine (9/9 unit tests + demo + e2e CLI) |
| Next | 🟢 **Phase 1** — media toolbelt (FFmpeg/Whisper) + MCP tool layer + Claude brain (Section A clip pipeline) |

**↳ Planning complete + decisions made. Owner chose: all-3 money models (sectioned), third-party-freely sourcing (with retained guardrails), Python+PySide6, CPU-only MVP. Rationale in [`06-decisions-and-product.md`](06-decisions-and-product.md).**

### Phase 0 result (2026-06-22)
The orchestration skeleton is built and **verified**: SQLite job queue + the linear state machine (`ingest→transcribe→find_highlights→clip→caption→compose→[approval gate]→publish→done`), per-stage retries → NEEDS_ATTENTION, idempotency keys, the **human approval gate** (default ON), **copyright-strike tracking** that auto-pauses a channel at 2 strikes, third-party-rights logging, and a full event audit trail. Stage runners are **stubs** (Phase 1 swaps in real FFmpeg/Whisper/Claude). Code in [`../src/`](../src/); run `python -m clippilot demo`.

### Code-audit + video-understanding (2026-06-22)
Ran a **code-level audit of all 32 repos** (workflow `wxozf83w8`, 35 agents reading actual source — dossiers in [`research/01-code-audit-dossiers.json`](research/01-code-audit-dossiers.json)). It rewrote [`03-tool-inventory.md`](03-tool-inventory.md) (now code-verified), produced [`02b-architecture-update.md`](02b-architecture-update.md), and the new [`07-video-understanding.md`](07-video-understanding.md). Key findings: AnimateAnyone is an **empty placeholder** (no code); only **FFmpeg + moviepy** are unconditional KEEPs; real understanding capability lives in **OpenMontage** (AGPL → clean-room port), **LocalAI** (Docker-only on Windows), and **palmier-pro** (macOS → reference only). Architecture decisions locked: **MCP over local HTTP `127.0.0.1`** (palmier-pro pattern), a first-class **Video Understanding Engine**, and a **pinned Python 3.12 venv** for the perception deps. The engine code was refined to the audited pipeline (`ingest → extract_signals → transcribe → understand → find_highlights → clip → caption → compose → [approval] → publish`) with the new `Understanding` model + likeness-guardrail wiring — **12/12 tests pass**.

### Phase 1 progress (2026-06-22) — real `extract_signals`
First real extractor stage is live: [`src/clippilot/media/`](../src/clippilot/media/) does genuine **ffmpeg** work — `probe` (metadata via ffprobe-json or ffmpeg-stderr fallback), `detect_scenes` (scene filter), `detect_silence` (silencedetect), `measure_loudness` (ebur128), `extract_keyframe` (downscaled JPEG). ffmpeg is bundled via **imageio-ffmpeg** (no manual install). Verified against an ffmpeg-generated sample clip — **19/19 tests pass** (7 new real-media tests). The `extract_signals` runner is now real (degrades gracefully when the source/ffmpeg is absent, so stub-path tests stay green).
**More Phase 1 (2026-06-22):** added the **scene-aware keyframe sampler** ([`media/sampler.py`](../src/clippilot/media/sampler.py)), the **`understand_video` assembly** ([`understand.py`](../src/clippilot/understand.py)) that fuses signals + keyframes into a real `Understanding`, and the **in-app MCP-over-HTTP server** ([`mcp_server/`](../src/clippilot/mcp_server/), JSON-RPC 2.0, loopback-only, tool surface `probe`/`extract_signals`/`understand_meta`/`understand_video`). The `understand` runner now calls the real assembly (graceful demo fallback when no media). **28/28 tests pass.**
**Claude brain shipped (2026-06-22):** [`src/clippilot/brain/`](../src/clippilot/brain/) — the **vision-pass video understanding** the owner asked for. `understand_video` now (when a key is set) sends the sampled keyframes to **Claude's own vision** (`claude-opus-4-8`, configurable to `claude-sonnet-4-6` for cost) and fills the semantic fields: `summary`, per-scene `visual_desc`, on-screen text (OCR-by-vision), mood, topics/entities, and **highlight candidates with reasons** — plus it sets the **likeness flag** when a real identifiable person appears (auto-routes to the consent guardrail). Built on the official `anthropic` SDK (v0.111, imports on 3.14), structured-JSON output via `output_config.format`, **mockable** so the suite is green without a key. Honest cost ≈ **$0.15–0.30 per 10-min video** at 6 frames/min. **35/35 tests pass.** Live runs need `ANTHROPIC_API_KEY` (env or gitignored `.env` — see [`.env.example`](../.env.example); never pasted in chat).
**Editing + transcription shipped (2026-06-22):** real **clip/caption/compose** via ffmpeg ([`media/edit.py`](../src/clippilot/media/edit.py)) — `clip_segment` cuts + reframes to vertical **9:16 (1080×1920, verified)**, `burn_subtitles` does libass caption burn-in, `concat_clips` stitches — and real **transcribe** via **faster-whisper** ([`media/transcribe.py`](../src/clippilot/media/transcribe.py), int8 CPU, verified end-to-end with the tiny model). **faster-whisper + ctranslate2 4.8 import on Python 3.14 — the 3.12 venv is NOT needed.** Wired: the `clip` runner emits real vertical clips from highlight spans; the `transcribe` runner runs whisper; per-scene transcript excerpts feed the brain. **45/45 tests pass** (1 real-whisper test gated behind `CLIPPILOT_RUN_WHISPER=1`).
### Code reuse from the OSS repos (2026-06-22)
Ran a code-level **harvest** of the 10 permissively-licensed repos (workflow `wb7rzn152`) → the per-stage **reuse map** in [`08-reuse-map.md`](08-reuse-map.md) (raw in [`research/02-harvest.json`](research/02-harvest.json)). First integrations landed: [`src/clippilot/publish/`](../src/clippilot/publish/) — the **publish stage is now built from MoneyPrinterTurbo (MIT) code**: `metadata.py` (ported `generate_social_metadata` + `SOCIAL_PLATFORMS` + hashtag normalization, LLM routed through ClipPilot's Claude brain) and `upload_post.py` (ported `UploadPostService` cross-poster on httpx, AI-disclosure forced on). The `publish` runner generates metadata (Claude/fallback) + cross-posts when an Upload-Post key is set. **54/54 tests pass.** The reuse map documents every remaining stage's source repo + the deliberate exclusions (AGPL/GPL/GPU/macOS/paid) — answering "are we leveraging the repos?": yes, where license + platform allow.

### Native Windows GUI shipped (2026-06-22)
[`src/clippilot/ui/`](../src/clippilot/ui/) — the **PySide6 desktop app** (the headline "native windows application"). Launch: `python -m clippilot.ui`. Tabs: **Dashboard** (live counts + recent jobs), **Section A/B/C** (each with an add-job form → enqueues into the engine), and **⛔ Review / Approve** — the approval gate: pick a parked job, see its clip path / Claude summary / generated title+caption+hashtags / **likeness + third-party warnings**, preview the clip (Open), and **Approve & publish** or **Reject**. System-tray icon; the engine drains on a **background thread** (own SQLite connection, WAL) so the Qt UI never blocks. Verified by an **offscreen construction test** (`QT_QPA_PLATFORM=offscreen`). **59/59 tests pass.** PySide6 6.11 (abi3) installs on 3.14.

### Captions + TTS shipped (2026-06-22) · project moved
**Project now lives at `C:\Priyansh\Money making\ClipPilot`**; the 32 OSS repos moved to `C:\Priyansh\Money making\reusablecode\`. Launchers added at the project root: `run.bat` (GUI), `demo.bat`, `cli.bat`; see `RUN.md` + `VISION.md`.
Two more reuse integrations (zero new pip deps): [`media/captions.py`](../src/clippilot/media/captions.py) — a **faithful port of remotion's `createTikTokStyleCaptions`** (MIT), verified against remotion's exact test vector (words→tokens→TikTok pages→clip-local SRT cues); and [`media/tts.py`](../src/clippilot/media/tts.py) — permissive **Windows SAPI TTS** via PowerShell `System.Speech` (no GPL edge-tts, no pip dep, no model download), with length-weighted word timing for karaoke captions. Real SAPI synthesis verified. **68/68 tests pass.**

### Claude-app connection shipped (2026-06-22) — Claude can now *operate* ClipPilot
[`src/clippilot/mcp_server/`](../src/clippilot/mcp_server/) grew from a read-only skeleton into the **control surface the owner asked for** ("connect with my claude app via custom mcp or cli so that claude can perform the task"). The MCP server now exposes **pipeline-control tools** on top of the 4 read/understand tools: `enqueue`, `run_engine`, `list_jobs`, `job_detail`, `job_counts`, `approve`, `reject`, `requeue`, `record_strike` — so Claude can run the whole money pipeline end-to-end (`enqueue → run_engine → review at the gate → approve/reject → run_engine → published`). Added a **stdio transport** ([`mcp_server/stdio.py`](../src/clippilot/mcp_server/stdio.py)) — the JSON-RPC-over-stdin/stdout that the **Claude Desktop app** launches via `python -m clippilot.mcp_server` — alongside the existing loopback HTTP server (`--http`). Stdlib-only (no `mcp` SDK), imports on 3.14; control tools run against the real SQLite store (CLIPPILOT_DATA-overridable) with per-call connections that close cleanly (Windows file-lock safe). **Setup guide + Claude Desktop config + tool reference in [`09-claude-app-connection.md`](09-claude-app-connection.md).** Verified end-to-end as a real stdio process (enqueue→run→approve→publish) and by unit tests. **76/76 tests pass** (8 new: control-tool flow + stdio transport).

### Automation control plane shipped (2026-06-22) — unattended 24/7 drain
The "100% automation" engine room, honestly scoped. [`src/clippilot/service.py`](../src/clippilot/service.py) — an **unattended service runner**: `run_cycle()` (one drain pass, env-aware store, re-reads settings each call so an `auto_approve` toggle takes effect immediately) + `run_service(interval, max_cycles)` (loop until stopped) + **Windows Task Scheduler** install (`--install --every N` writes a `service_tick.bat` wrapper and registers a `schtasks` MINUTE task, so Windows keeps draining across reboots). Two new MCP tools — **`get_settings` / `set_settings`** — let Claude (or the GUI) graduate a trusted lane to hands-off (`auto_approve`) or tune guardrails, persisted to the shared `settings.json`. Config gained env-aware path helpers (`db_path()/settings_path()/data_dir()`) so the MCP server + service resolve `CLIPPILOT_DATA` at runtime. **Verified end-to-end**: `set_settings auto_approve=true → enqueue → service --once → published` (9 stages → `done`, no human gate); and the gate stays ON wherever auto_approve is off. **84/84 tests pass** (8 new: settings tools + service loop + Task-Scheduler command builders). Connection + automation guide: [`09-claude-app-connection.md`](09-claude-app-connection.md) §6.

### Karaoke captions wired into the engine (2026-06-22) — clips are now captioned
The `caption` stage is no longer a stub — it burns **karaoke captions into every clip**, the format short-form actually needs. `transcribe` now emits faster-whisper **word-level timings** (`word_timestamps`, flat `words` list + per-segment words); `clip` records each clip's source span; the new **`_caption` runner** ([`runners.py`](../src/clippilot/runners.py)) takes the words overlapping each clip, groups them via the remotion-port [`media/captions.py`](../src/clippilot/media/captions.py) `cues_for_clip` (→ TikTok pages → **clip-local offsets**), writes SRT, and burns them in with libass ([`media/edit.py`](../src/clippilot/media/edit.py) `burn_subtitles`). Captioned clips become what compose/publish/GUI use (with graceful passthrough when there's no speech / ffmpeg). **Verified with a real ffmpeg burn** (synthetic word-timings → a captioned .mp4 on disk). **90/90 tests pass** (6 new: `cues_for_clip`, the caption runner stub+real-burn, captioned-clip preference).

### Free first-party YouTube publisher shipped (2026-06-22) — publish without the paid gateway
[`src/clippilot/publish/youtube.py`](../src/clippilot/publish/youtube.py) — a hand-rolled **YouTube Data API v3** uploader (OAuth2 refresh-token → access token, then a resumable `videos.insert`) on httpx, **no `google-api-python-client` dep**. It's the FREE alternative to the paid Upload-Post gateway, so the `publish` runner now picks **first-party YouTube → Upload-Post → manual** in that order. AI-disclosure honored by appending a disclosure line to the description (YouTube's altered-content flag isn't in the public Data API). Configured via `YOUTUBE_CLIENT_ID/SECRET/REFRESH_TOKEN` (one-time Google Cloud OAuth setup — [`09-claude-app-connection.md`](09-claude-app-connection.md) §7; `.env.example` updated). `dry_run` builds the request body without creds (used by tests). **94/94 tests pass** (4 new: body shape/caps, disclosure append + dedupe + disable, env gating).

### Section B (Faceless Funnel) generation shipped (2026-06-22) — a short from just a topic
The honest "press start" path is real. New [`src/clippilot/generate/`](../src/clippilot/generate/) (Pixelle `StandardPipeline` recipe, Apache): `script.py` (topic → **original** Claude script with the transformative-nudge guardrail, template fallback), `assemble.py` (ffmpeg narration-over-background → vertical 1080×1920 mp4), `pipeline.py` (`generate_short`: script → SAPI TTS → probe → assemble). Wired into the engine: the **`_ingest` runner** now sets a `media_path` for every job — for Section B/C it **generates** the short from the topic (`source_ref`); a new `_media_path(job)` threads that working media through `extract_signals`/`transcribe`/`understand`/`clip`, and `clip` treats a generated short as one full-length clip. So Section B flows topic → generated video → transcribe → **karaoke captions** → approve → publish, identical downstream to Section A. **Verified with REAL SAPI TTS + ffmpeg** (generated a playable narrated short end-to-end). **103/103 tests pass** (9 new: script gen/fallback, assemble, real generate_short, Section-B ingest, media_path threading).

### YouTube OAuth helper shipped (2026-06-22) — free publishing is one command away
[`src/clippilot/publish/youtube_auth.py`](../src/clippilot/publish/youtube_auth.py) — the one-time **installed-app OAuth loopback flow** that gets the `refresh_token` the free first-party publisher needs: `build_auth_url` (offline+consent so a refresh token is issued), a loopback `HTTPServer` that catches the redirect, `parse_code`, `exchange_code`, and `append_env` (saves `YOUTUBE_REFRESH_TOKEN` to `.env`). One command: `python -m clippilot.publish.youtube_auth --write-env` (reads client id/secret from `.env`). Pure helpers unit-tested; the live browser flow is the only un-mocked bit. **110/110 tests pass** (7 new: auth-URL params, code/error parsing, .env replace/append, CLI credential guard). Free-publish setup is now: Cloud-Console client → this command → done ([`09-claude-app-connection.md`](09-claude-app-connection.md) §7).

### GUI Settings tab shipped (2026-06-22) — operate the automation from the app
The automation control plane is now in the window the owner double-clicks. New **⚙ Settings tab** ([`ui/app.py`](../src/clippilot/ui/app.py) `SettingsTab`): an **auto-approve** toggle (with the prominent per-lane warning), the human-gate toggle, strike-pause threshold, and brain model — saved via `AppController.get_settings`/`save_settings`, which persist to `settings.json` (shared merge logic now in [`config.py`](../src/clippilot/config.py) `merge_settings`, reused by the MCP `set_settings` tool) and apply to the live queue. The **background worker re-reads settings each cycle** ([`ui/worker.py`](../src/clippilot/ui/worker.py)) so flipping auto-approve takes effect on the next drain — no restart. Locked guardrails (AI-disclosure, no real-person face-swap) shown read-only. Refreshed once on open so the periodic timer never stomps an in-progress edit. **111/111 tests pass** (settings round-trip + 6-tab offscreen construction).

### Real `find_highlights` shipped (2026-06-22) — the brain's picks become a clean clip plan
[`src/clippilot/highlights.py`](../src/clippilot/highlights.py) `plan_highlights` refines the understanding's `highlight_candidates` into the spans actually cut: enforce short-form **duration bounds** (≈15–60s; trim long, extend short, clamp to source), **greedy non-overlap** selection ranked by Claude's score, **top-N** cap, chronological order, each span carrying score+reasons+duration. The `find_highlights` runner now uses it (generated shorts → whole-video span; no-candidates → an early fallback span); `clip` prefers this refined plan over raw candidates. Better moment selection = better-performing shorts. **117/117 tests pass** (6 new: ranking, overlap-drop, duration trim/extend, source-clamp, invalid-span filtering). With this, the **only remaining core-pipeline stub is `compose`** (multi-clip stitch — optional for single-clip shorts).

### Real-media end-to-end proven (2026-06-22) — the money pipeline works on real input
[`src/tests/test_e2e_integration.py`](../src/tests/test_e2e_integration.py) — a self-contained REAL run (no mocks, no key): SAPI synthesizes a narrated clip → ffmpeg muxes a source video → the **whole engine drains with real ffmpeg + faster-whisper** → asserts each stage produced real output and a **real captioned vertical clip lands on disk**, then approve → publish → DONE. **Ran green in 6.2s** (`CLIPPILOT_RUN_E2E=1`, tiny whisper). The runner now reads `CLIPPILOT_WHISPER_MODEL` (default `base`, `tiny` for fast/CI). Opt-in so the default suite stays fast: **118/118 tests pass, 2 skipped** (the opt-in whisper + e2e tests). This validates the full chain — ingest→signals→transcribe→understand→highlights→clip→caption→publish — on genuine media, not just unit mocks.

### `clippilot doctor` shipped (2026-06-22) — the path to first dollar, at a glance
[`src/clippilot/doctor.py`](../src/clippilot/doctor.py) `check_readiness` inspects the environment and reports what's configured (**ffmpeg, faster-whisper, SAPI, Anthropic key, YouTube creds, Upload-Post, PySide6**), what each unlocks, derived capabilities (clip / captions / generate-B / brain / publish-free / publish-paid), and **ordered next steps to first dollar**. Exposed as `python -m clippilot doctor` (CLI, ASCII-safe) and the **`doctor` MCP tool** so Claude can run it from the desktop app and guide setup. Live run confirms ffmpeg/whisper/SAPI/PySide6 ready; clear next-steps for the optional keys. **124/124 tests pass** (6 new: report structure, no-creds guidance, creds-enable-publish, ASCII-safety, MCP tool, CLI).

### `compose` wired + README refreshed (2026-06-22) — zero stubs left in the pipeline
The last core stub is gone: [`runners.py`](../src/clippilot/runners.py) `_compose` finalizes the deliverables — **passthrough** by default (each captioned clip is its own short, the Shorts/Reels norm), or, when `Settings.compose_concat` is on and there are ≥2 real clips, **stitches them into one compilation** (`media/edit.concat_clips`). `publish` + the GUI now prefer compose output → caption → bare cuts. **Every core pipeline stage is now real — no stubs.** Also rewrote the **stale Phase-0 README** into a real operator quickstart (doctor → run → add job → approve → free-publish setup → MCP → 24/7 service). **129/129 tests pass** (5 new: compose passthrough, no-clips stub, concat-flag fallback, real-ffmpeg concat, compose-first preference).

### Section B visuals upgraded (2026-06-22) — gradient background + title card
Generated shorts no longer sit on flat black. [`generate/assemble.py`](../src/clippilot/generate/assemble.py) now renders an **animated `gradients` backdrop** + a centered top **title card** (ffmpeg `drawtext`, the script's title wrapped to ≤3 lines), with a robust **graceful fallback** to solid color if the ffmpeg build lacks drawtext/freetype. To dodge Windows path-escaping it uses the `burn_subtitles` pattern (cwd = a work dir holding a copied `font.ttf` + `title.txt`, referenced by bare name). Capability-probed at runtime (`_has('drawtext'/'gradients')`, font discovery). The bundled ffmpeg 7.1 supports all three; **verified rendering a real 1080×1920 gradient short with a title card**. `generate_short` passes the script title through. **132/132 tests pass** (3 new: title wrap, title-card render, solid fallback).

### Deterministic highlights shipped (2026-06-22) — smart free clips, no key needed
[`highlights.py`](../src/clippilot/highlights.py) `signal_highlights` derives clip candidates from the **cheap ffmpeg signals** (silence segments + scene cuts): it slides a target-length window across the video, scores each by **speech density** (fraction not silent), and nudges windows that start near a scene change — then `plan_highlights` ranks/dedupes/caps. The `find_highlights` runner uses it when there are **no Claude candidates** (e.g. no `ANTHROPIC_API_KEY`), replacing the dumb "first 45s" fallback with genuinely-chosen moments. So Section A makes decent clips for **free**, and pays for Claude only when you want the human-like read. **Re-verified on real media** (e2e: the no-key signals path produced a real captioned clip in 5.3s). **136/136 tests pass** (4 new: speech-density scoring, scene-cut bonus, zero-duration, feeds-planner).

### GUI service-control panel shipped (2026-06-22) — turn on 24/7 from the app
The ⚙ Settings tab gained an **Unattended automation** group: a **Check status / Install 24/7 (every 5 min) / Remove** button row + an auto-approve status line, so a non-CLI operator can enable the round-the-clock Windows task from the app. Wired through new `AppController.automation_status / install_service_task / remove_service_task` (delegating to `service.task_installed / install_task / uninstall_task` — new `build_status_command` + `task_installed`). Task status is checked lazily (only on button press) so the GUI never spawns schtasks on the refresh timer. Also exposed the **`compose_concat`** toggle in Settings. **139/139 tests pass** (3 new: status-command builder, install/remove delegation, automation_status — schtasks mocked, no real tasks created; offscreen construction still green with the bigger Settings tab).

### Section B funnel path proven end-to-end (2026-06-22) — both money paths verified
Added a **Section B full-pipeline e2e** to [`test_e2e_integration.py`](../src/tests/test_e2e_integration.py): a TOPIC (no source video) → `_ingest` **generates** a real narrated gradient+title short → real-whisper transcribes the narration → understand → find_highlights (whole-video) → clip → **captions burned** → approve → publish → DONE. **Both e2e tests pass green** (Section A real-video + Section B topic-to-short, 18s with tiny whisper). So **both money paths are validated end-to-end through the whole engine on real media**: A (clip authorized long-form) and B (generate from a topic). **140/140 tests pass, 3 opt-in skipped.**

### Batch enqueue (2026-06-22)
[`src/clippilot/batch.py`](../src/clippilot/batch.py) — point ClipPilot at a folder and it enqueues every video (DFY throughput), idempotent per file path so re-scanning is safe. CLI `enqueue-folder` + MCP `enqueue_folder`. **145 tests.**

### ⭐ Section B B-ROLL — fixed "ugly slop" (2026-06-22, owner feedback)
**Owner feedback: the generated videos were "ugly slop … not understanding the content."** Root cause: Section B put narration+captions over a flat gradient. Fix (harvested from the OSS repos as requested): [`src/clippilot/generate/broll.py`](../src/clippilot/generate/broll.py) now sources **relevant B-roll** — **free Bing image search** (no API key; ShortGPT technique) and **Pexels portrait stock video** when `PEXELS_API_KEY` is set (MoneyPrinterTurbo technique) — keyed to search terms extracted from the script (Claude when available, else a stopword heuristic). [`generate/assemble.py`](../src/clippilot/generate/assemble.py) gained a **Ken-Burns image slideshow** (`assemble_slideshow`) and a **stock-video montage** (`assemble_broll_video`), both 9:16, timed to the narration; the gradient is now only a last-resort fallback. `generate_short` records which `visual` it used. **Verified on real network: a topic → 5 real Bing images → a 1080×1920 Ken-Burns short with narration** (visual=`image_slideshow`), not a gradient. **153/153 tests pass** (8 new: keyword heuristic/LLM, Bing-HTML parse, fetch fallback, slideshow + b-roll-video composition). Honest note: genuine *content understanding* + non-generic scripts still need the **Claude brain (ANTHROPIC_API_KEY)** — b-roll makes it watchable; the key makes it smart.

### Per-caption timed B-roll (2026-06-22) — visuals change with the narration
Upgraded Section B from one slideshow to **a fresh, phrase-relevant image per spoken caption** (ShortGPT technique). [`broll.py`](../src/clippilot/generate/broll.py) `phrase_windows` derives caption windows from SAPI proportional word-timing (no whisper at gen time); `fetch_timed_broll` fetches a topic+salient-word image per phrase (deduped, bounded); [`assemble.py`](../src/clippilot/generate/assemble.py) `assemble_timed_slideshow` Ken-Burns-renders each image for its exact window. `generate_short` prefers `timed_broll` → even slideshow / stock video → gradient. **Verified on a real render: an ocean script → 11 phrase windows → 10 distinct ocean images, a 1080×1920 short whose visuals cut with each phrase** (visual=`timed_broll`). **157/157 tests pass** (4 new: salient word, phrase windows, timed fetch, timed slideshow). NOTE: suite is now ~110s (many real renders) — heaviest render tests may move opt-in.

### Caption styling upgraded (2026-06-22) — big bold outlined captions (was tiny)
The burned captions were tiny/plain (SRT + `FontSize=24`). Now [`media/edit.py`](../src/clippilot/media/edit.py) writes a **styled ASS** with explicit `PlayResX/Y` = the clip size and a **big (FontSize 92), bold, thick-outlined, bottom-centre, UPPERCASE** style — the punchy short-form look (VideoCaptioner/ShortGPT approach), pixel-accurate vs SRT force_style. `write_ass` + `_ass_ts` added; `burn_subtitles` handles `.ass` (uses the file's own styles) and `.srt` (force_style); the `_caption` runner now emits ASS. Affects **all** output (Section A + B). **Verified on a real render** (1080×1920 clip → big bold captions burned). **161/161 tests pass** (4 new: ASS timestamp, PlayRes+big-font generation, uppercase toggle, ASS burn).

### Karaoke captions (2026-06-22) — the active word pops as it's spoken
The signature TikTok look. [`media/captions.py`](../src/clippilot/media/captions.py) `pages_for_clip` keeps per-token timing (clip-local); [`media/edit.py`](../src/clippilot/media/edit.py) `write_ass_karaoke` + `_karaoke_text` emit ASS `\k` tags so each word turns the **highlight colour (bright yellow) exactly when it starts being spoken** over a white base — progressive karaoke fill. The `_caption` runner now writes karaoke ASS. **Verified on a real render**: `{\k0}DEEP {\k50}OCEAN {\k60}CREATURES` burned onto a 1080×1920 clip, \k timings matching the word spans. **165/165 tests pass** (4 new: pages_for_clip clip-local tokens, \k-gap timing, karaoke colour+PlayRes).

**Quality recap (all owner-feedback-driven, each verified by a REAL render):** gradient slop → content-matched B-roll → per-phrase timed visuals → big bold captions → **karaoke word-pop**. 

### Music bed + faster tests (2026-06-22)
**BGM (guardrail-safe):** [`media/edit.py`](../src/clippilot/media/edit.py) `add_bgm` mixes a **looped, low-volume music bed under the narration** — but only a **user-supplied** track (`Settings.bgm_path`/`bgm_volume`, exposed in the GUI Settings tab; never auto-fetched, honoring the cleared-music guardrail). Section B's `generate_short`/`_ingest` apply it when set. **Faster tests:** the heaviest/networked render tests (TTS generation, b-roll fetch, slideshow/edit renders) are now gated behind **`CLIPPILOT_RUN_RENDER=1`**, so the **default suite dropped from ~110s → ~45s** (167 tests, pure logic + light checks) while the full render verification runs on demand (and in the e2e). **Verified:** add_bgm renders a real mixed-audio short; default + render suites both green.

### No-key script variety + flaky-test fix (2026-06-22)
[`generate/script.py`](../src/clippilot/generate/script.py) `_fallback` now rotates hook/body/CTA templates keyed off the topic, so no-key scripts aren't identical boilerplate (still honestly a template — a real topic-specific script needs the Claude brain). Fixed a **flaky render test**: the two TTS render tests did live Bing fetches and hiccuped under marathon load — b-roll fetch is now **stubbed off in those tests** (deterministic gradient path; b-roll itself is covered by mocked unit tests + manual renders). **Default suite 168 tests (~50s); the render suite (`CLIPPILOT_RUN_RENDER=1`) is now deterministic — 46 render tests green in 16s.**

### `demo-short` command (2026-06-22) — watch a finished short in one line
[`cli.py`](../src/clippilot/cli.py) `cmd_demo_short`: `python -m clippilot demo-short "<topic>"` runs the **full** Section-B pipeline (generate → timed b-roll → narration → whisper → karaoke captions) and prints the finished `.mp4` path (`--open` to auto-open). `_ingest` now propagates the `visual` field so it reports what was used. **Ran for real**: topic "3 mind blowing facts about space" → a **1080×1920, 13.6s, h264 short with narration + 10 distinct space b-roll images (one per phrase) + karaoke captions, 3.15 MB** at `data/media/clips/job4_00_cap.mp4`. **171/171 tests pass** (CLI parser wiring tests added).

### Review-screen thumbnail (2026-06-22)
The ⛔ Review/Approve screen now shows an **inline preview frame** of the selected clip (not just text), so approve/reject is visual. [`ui/controller.py`](../src/clippilot/ui/controller.py) `thumbnail_for` extracts a cached mid-clip keyframe (ffmpeg); [`ui/app.py`](../src/clippilot/ui/app.py) ReviewTab renders it (best-effort, falls back to a placeholder). **Verified** the extraction on the real demo short (`job4_00_cap_thumb.jpg`). **172/172 tests pass.**

### Section B/C tab clarity (2026-06-22)
The generated sections now read as generators: Section B/C show **"Topic for the short…"** + a **"Generate short"** button (no Browse/rights — generated content is owned), while Section A keeps the source-video form + Browse + rights. Verified in the offscreen construction test. **172/172 tests pass.**

### Pexels Photo API for b-roll (2026-06-22)
[`generate/broll.py`](../src/clippilot/generate/broll.py) `image_urls` now prefers the **Pexels Photo API** (curated, high-res, portrait) when `PEXELS_API_KEY` is set, falling back to free Bing image search otherwise (`pexels_photo_urls` + pure `_parse_pexels_photos`). All image fetchers (slideshow + per-caption timed) route through it. Verified the Bing fallback still fetches live + the timed pipeline still produces segments; Pexels path covered by unit tests. `.env.example` documents the optional key. **175/175 tests pass** (3 new: Pexels parse, source preference with/without key).

### Adversarial code review → 12 real bugs fixed (2026-06-22)
Ran a multi-agent review (5 reviewers × find→adversarially-verify; 21 agents) over the media/ffmpeg/network/engine/captions/MCP code. **12 confirmed bugs (no false positives), all fixed:**
- **engine/concurrency (high/med):** `claim_next` was a non-atomic SELECT+UPDATE → two drainers (GUI worker + service) could claim & **double-publish** the same job → now an atomic conditional `UPDATE ... WHERE status='pending'` (rowcount==1 wins); a `complete_stage` failure *after* a successful runner re-ran PUBLISH (double upload) → engine now splits the try and parks at NEEDS_ATTENTION via new `mark_needs_attention` (no retry of irreversible stages); `record_strike` read-modify-write lost strikes → atomic `strikes = strikes + 1`; `ensure_channel` SELECT-then-INSERT race → `INSERT OR IGNORE`.
- **MCP (high/med):** a valid-but-non-object JSON line (batch array / scalar / null) crashed the **whole stdio server** and broke HTTP responses → `handle_rpc` now guards non-dict → `-32600`, and the stdio loop wraps dispatch so one bad message can't kill it.
- **ffmpeg/media (high/med/low):** `concat_clips` crashed on any video-only clip (`[i:a]` missing) → now adds a silent track to audioless clips first; `assemble_short` wrote to the wrong path under a relative `out` (cwd changed) → resolves out/audio/image to absolute; `_srt_ts` could emit an invalid `,1000` ms field → rounds once with carry; single-clip concat skipped `mkdir` → hoisted.
- **network/captions (med/low):** YouTube upload buffered the whole video in RAM (`fh.read()`) → streams `content=fh`; a whitespace-only final caption token stranded the last page at `duration_ms=inf` → finalized.
**180/180 tests pass** (5 new regression tests: atomic claim, publish-idempotency, `_srt_ts` carry, caption trailing-ws, non-object RPC). Render tests re-verified.

### Code review round 2 → 13 more bugs fixed (2026-06-22)
Second multi-agent review (20 agents) over publish/brain/ui/misc. **13 confirmed bugs (no false positives), all fixed:**
- **compliance/money (high):** YouTube AI-disclosure was appended *then* truncated by the 5000-char cap → now reserves room (and dedups on the real marker, not the substring "AI-generated"); the Upload-Post cross-poster never set TikTok's `is_aigc` disclosure → added; `batch` idempotency key used `abspath` without `normcase` → the same Windows file via different-cased paths enqueued (and could **publish**) twice → `normcase`; the `ai_disclosure_applied` audit flag didn't match reality → now truthfully `True` (forced).
- **robustness (high):** a Claude vision/API failure (rate-limit/timeout/refusal) **crashed the understand stage** instead of falling back to the deterministic read → wrapped in try/except; an unknown/typo'd guardrail key from the MCP `set_settings` tool crashed `Settings.from_dict` → now filters to known fields + tolerates non-dict; `metadata` greedy `{.*}` JSON recovery swallowed trailing prose, and a JSON-list response crashed with AttributeError → `raw_decode` + `isinstance` guard.
- **GUI (high/med):** the **approval list selection was silently dropped on every 2s auto-refresh** → Approve/Reject/Open became no-ops → now restores the selected job by id; thumbnail ffmpeg ran on the GUI thread (freeze) → moved to a `QThreadPool` with a queued-signal result.
- **brain/low:** `frame_cap` (120) exceeded the 100-image vision cap (wasted frames, empty scene descs) → clamped; `normalize_hashtags(count=0)` returned 1; YouTube disclosure skipped if the caption merely contained "AI-generated".
**188/188 tests pass** (8 new regression tests). **Across both reviews: 25 real bugs found and fixed.**

### DFY templates (profiles) shipped (2026-06-22)
[`src/clippilot/profiles.py`](../src/clippilot/profiles.py) — named per-client presets (`Profile`: section, rights, channel + voice/bgm_path/target_seconds for generated Section B/C), stored as `data/profiles.json` (load/save/get/delete). `enqueue_with_profile` applies the section/rights/channel and carries the generation overrides in the job payload; the `_ingest` runner reads them so every job for a client inherits the same voice/music/length. Exposed as MCP tools (`list_profiles`, `save_profile`, `delete_profile`, `enqueue_with_profile`) so Claude can manage client templates. Supports the recurring **done-for-you** B2B money model. **194/194 tests pass** (6 new: store CRUD, enqueue-with-profile payload, MCP tools, corrupt-file tolerance).

### Profile CLI (2026-06-22)
DFY templates are now usable from the CLI too: `profile-save` / `profile-list` / `profile-delete` / `enqueue-profile` ([cli.py](../src/clippilot/cli.py)), alongside the MCP tools. **196/196 tests pass** (2 new: parser wiring + a save→list→enqueue→delete round-trip).

### RUN.md refreshed into a full operator guide (2026-06-22)
Rewrote the (stale) RUN.md to cover the real app: `doctor`, `demo-short`, the GUI tabs + preview thumbnail + ⚙ Settings, free-publish setup (`youtube_auth`), the 24/7 service, DFY profiles + batch enqueue (CLI), the MCP connection, and the Claude-key note. Every command is test-covered.

### GUI Templates tab shipped (2026-06-22) — DFY profiles complete across all surfaces
[`ui/app.py`](../src/clippilot/ui/app.py) `ProfilesTab` (📋 Templates) — create/list/delete client templates in the app (name, section, channel, voice, length), via new `AppController.list_profiles/save_profile/delete_profile`. Refreshed on open + after save/delete (not on the timer, so the table selection isn't reset). **DFY templates are now usable from MCP, CLI, AND the GUI.** **197/197 tests pass** (7-tab offscreen construction + controller profiles CRUD).

### ✅ Full verification gate GREEN (2026-06-22) — feature-complete
Ran the complete suite with **all render + e2e tests enabled** (real ffmpeg + whisper + TTS): **197 tests OK** in ~95s, only the opt-in `CLIPPILOT_RUN_WHISPER` test skipped. Both money paths (Section A real-video → captioned clip; Section B topic → generated short) and every composition/caption/b-roll/publish/profile/GUI path are verified green on real media after the full build + 25 bug fixes. VISION §9 + header updated to "feature-complete & verified".

### Code review round 3 → 5 more bugs fixed (2026-06-22)
Third review over the un-reviewed glue (cli/profiles/generation/sampler/tts). **5 confirmed bugs, all fixed:**
- **security (high):** `media/tts.py` interpolated the `voice` value straight into the SAPI **PowerShell** command → **command injection** reachable from a DFY profile / MCP request / CLI / GUI → now single-quote-escaped (`_ps_lit`, applied to voice + wav path); SAPI synthesis re-verified working.
- **automation (high):** `service.build_install_command` pre-quoted the `/TR` path, which `subprocess` then double-escaped → the **24/7 scheduled task silently never ran** → pass the bare path.
- **(med):** the CLI surfaced `ValueError`s (e.g. `approve 999`) as raw tracebacks → `main()` now prints a clean error + exit 1; `--install` didn't propagate `CLIPPILOT_DATA` → the task drained the wrong store → now forwarded; `_clip` wrote to the import-time `cfg.MEDIA_DIR` instead of env-aware `cfg.media_dir()` → fixed (matches its siblings).
**201/201 tests pass** (4 new regression tests). **Across all three reviews: 30 real bugs found and fixed.**

**Re-verified:** the full render+e2e gate is GREEN after the round-3 fixes (201 tests, real ffmpeg+whisper+TTS, both money paths, ~8.6 min, OK). The build is **feature-complete + hardened by three reviews (30 bugs fixed) + verified end-to-end on real media.**

### First-party Instagram/Facebook publisher shipped (2026-06-22)
[`src/clippilot/publish/instagram.py`](../src/clippilot/publish/instagram.py) — the free first-party IG/FB path via the Meta Graph API: the 3-step Reels flow (create container with `video_url` → poll `status_code` until FINISHED → `media_publish`), on httpx, AI-disclosure appended to the caption (reserving room within the 2200-char cap), `dry_run` + env-config (`INSTAGRAM_USER_ID`/`INSTAGRAM_ACCESS_TOKEN`). **Honest constraint (the previously-"blocked" item):** the Graph API fetches the video from a **public URL** — it doesn't accept a local upload — so this is exposed for operators who host their clips and is **not** in the engine's default auto-chain (which only has a local path); Upload-Post stays the no-hosting IG/FB option. **205/205 tests pass** (4 new: dry-run container shape, disclosure cap, missing-url, env gating). `.env.example` documents it.

**The publishing surface is now complete** (YouTube first-party + IG/FB first-party + Upload-Post cross-post). The Instagram publisher is exposed via the **`publish_reel` MCP tool + `publish-reel` CLI** (206 tests).

### New owner directive (2026-06-22): integrate ALL 32 apps' functionality
Built the authoritative **coverage map** [`10-functionality-coverage.md`](10-functionality-coverage.md): every one of the 32 apps is **Integrated** (FFmpeg, MoneyPrinterTurbo, ShortGPT, remotion, Pixelle, VideoCaptioner-recipe, + the YouTube/IG/FB publishers), **Addable backlog** (ffsubsync resync, ShortGPT facts-mode, hyperframes caption skins, backgroundremover cutout, cinematic prompt-builder, manim explainers, hosted-API gen hooks), or **Excluded-with-reason** (GPU-only / AGPL / GPL-desktop / iOS / Docker / paid / learning-only / duplicate). The loop now works the **Addable backlog** top-down.

**Next:** backlog #1 — **ffsubsync subtitle↔audio resync** (MIT, pure-numpy `FFTAligner`; skip its GPL auditok VAD).

### 2026-06-23 (later): palmier-pro parity + full app-by-app SOURCE sweep
Two more owner directives, both COMPLETE: **(1) palmier-pro feature parity** — re-authored palmier's defining capability (a Claude-drivable multi-track TIMELINE EDITOR) in Python/ffmpeg under [`../src/clippilot/editor/`](../src/clippilot/editor/): timeline model + clip ops (trim/split/ripple/overwrite), transform/crop/opacity/fade, **keyframe animation** (position/opacity/rotation/volume; linear/hold/smooth/spring + ease_in/out/there_and_back/rush_into/overshoot, RDP-simplified), multi-track ffmpeg compositing, **chroma-key**, **clip-to-clip transitions** (crossfade+slide), **per-clip color grading + film grain**, undo, EDL export, H.264/HEVC/ProRes, captions-into-timeline, generate-image/video-into-timeline — **16 editor MCP tools**. Adversarially hardened (10 bugs fixed). **(2) deep-mine EVERY reusablecode app's source** (palmier + 3 batch workflows + the original audit) — integrated RDP keyframe simplification, chroma key, spring + 5 manim easings, clip transitions, multi-intent script router (narrative/motion/montage), color grading/film grain; all portable functionality across all 32 repos is now integrated or documented-excluded (see [`12-deep-mine-log.md`](12-deep-mine-log.md), [`11-palmier-parity.md`](11-palmier-parity.md)). Test suite **206 → 377+**, render+e2e gate green, every render verified by eye.

### 2026-06-23: "all 32 apps" backlog CLEARED + hardened
Worked the [`10-functionality-coverage.md`](10-functionality-coverage.md) backlog to completion — every one of the 32 `reusablecode/` repos is now **Integrated** or **Excluded-with-reason**. Integrated this session (each tested; render-verified where it renders): **ffsubsync** resync, **ShortGPT** facts-mode, **caption skins** (hyperframes/VideoCaptioner, adversarially libass-verified), **cinematic prompt-builder** (Open-Generative-AI), **yt-dlp** source download (OpenMontage), **real motion-footage montage** (Pexels/Pixabay), **hosted image-gen hook** (GPU models' CPU stand-in), **subject cutout** (backgroundremover/rembg), **explainer clips** (manim's role, ffmpeg-native). Then an **adversarial multi-agent hardening pass** (13 agents) found+fixed **3 real bugs** (concat audio-stream fallback, motion-montage visual mislabel, gen-image HTTP status check). Catalog correction: confirmed exactly 32 repo dirs (added OpenMontage + palmier-pro to excluded).

Finally, a **real end-to-end demo render** surfaced off-topic b-roll (slop the tests couldn't catch) and it was root-caused + fixed: `_core_subject` strips "N facts about" framing; b-roll searches the core subject; an on-subject image **pool** rotates for generic caption pages; and — the key fix — **free Bing scraping (unreliable/bot-detected) was replaced by Openverse** (free, keyless, reliable, CC-licensed) as the preferred image source (`image_urls` = Pexels→Openverse→Bing). Re-render verified: real, varied, on-topic footage. Test suite **206 → 274**, render+e2e gate green.

## Document roadmap (what each planning file will contain)

| Doc | Title | Contents |
|-----|-------|----------|
| `01-feasibility-and-money-reality.md` | Feasibility & money reality | Verdict; honest 2025-26 Shorts/Reels payout reality; demonetization/ban crackdowns on mass-produced AI content; ranked money models; blunt year-1 earnings range + failure base-rate |
| `02-architecture.md` | Architecture | Native Windows shell choice; Claude-as-brain-via-MCP design; the MCP tool surface mapped to specific OSS tools; where Claude decides vs where deterministic code owns the loop; scheduler/queue for 24/7 unattended runs; cost-per-video model; ASCII component diagram |
| `02b-architecture-update.md` | Architecture update (post code-audit) | Revised MCP tool surface; MCP-over-HTTP decision; AGPL-safe OpenMontage port; palmier-pro reference; 3.12-venv + ffmpeg packaging; updated diagram with the Video Understanding Engine |
| `03-tool-inventory.md` | Tool due-diligence (**code-verified**) | Per-repo: license + commercial-use flag, Windows/CPU/GPU, integration method, concrete role; KEEP/OPTIONAL/DROP; video-understanding contributors |
| `04-legal-guardrails.md` | Legal guardrails | Red/Yellow/Green risk list; non-negotiable guardrails baked into the app |
| `05-build-plan.md` | Phased build plan | MVP-first roadmap, person-week estimates, cost per video/month, milestones |
| `06-decisions-and-product.md` | Decisions, product spec & setup | The 4 locked owner decisions; the 3-section product; third-party legal posture; toolchain findings |
| `07-video-understanding.md` | Video understanding (human-like) | `understand_video` MCP tool; modality→OSS-tool map; frame-sampling + honest Claude-vision cost model; likeness-guardrail wiring |
| `08-reuse-map.md` | Reuse map | Per-stage map of which OSS repo's actual code ClipPilot reuses (port/import/subprocess) + the documented exclusions (AGPL/GPL/GPU/macOS/paid) |
| `09-claude-app-connection.md` | Claude-app connection | The custom MCP (stdio + HTTP) + pipeline-control tool surface; Claude Desktop config; the end-to-end loop; honest "100% automation" via graduated per-lane auto-approve |

## Open decisions — ✅ ANSWERED (see `06-decisions-and-product.md`)

The gating decisions are resolved: **all-3 money models (sectioned)**, **third-party sourcing allowed** (guardrails on, no hard allow-list), **Python+PySide6**, **CPU-only MVP** (GPU generators dropped/hosted-API only), platforms **YouTube + IG/FB** via official APIs, and **no real-person face-swap** (hard line). One remaining input needed only when we reach the live Claude brain: an **Anthropic API key** (set via env var / local `.env`, never pasted in chat).

## Tool inventory snapshot (pre-research — superseded by code-verified `03-tool-inventory.md`)

32 repos in `C:\Priyansh\Money making\` (each nested `<name>\<name>\`):

- **AI video-gen models (GPU-heavy):** Open-Sora, Wan2.2, LTX-Video, AnimateAnyone, diffusers
- **Avatar / lip-sync / face-swap:** Duix-Avatar (offline Windows digital human), video-retalking, Deep-Live-Cam (real-time face swap)
- **Auto short-video pipelines:** MoneyPrinter, MoneyPrinterPlus, MoneyPrinterTurbo ×2, ShortGPT, Pixelle-Video
- **Agentic / programmatic render:** hyperframes (HeyGen, "built for agents"), remotion (React video), ViMax (agentic), manim
- **Editing libs/tools:** FFmpeg, moviepy, OpenShot, LosslessCut, backgroundremover, GPUImage (iOS — likely drop)
- **Captions/subs:** VideoCaptioner, ffsubsync · **Local AI serving:** LocalAI
- **Marketing-funnel repo:** Open-Generative-AI (MuAPI funnel) · **Learning-only:** Hello-Python, open-source-cs
